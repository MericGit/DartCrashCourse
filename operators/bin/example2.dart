void main(List<String> args) {
  ///There are 4 types of oprators
  ///Unary prefix, unary postfix, binary infix, compound assignment

  int age = 843;
  //Bitwise operators
  print(age & 327);
  print(age | 327);
  print(age << 2);
  print(age >> 2);
  //Ok printing the int conversion of a bit operation is stupid but just know
  //that bitwise works like C
  //Bit shifting and masking works too obviously.
}
